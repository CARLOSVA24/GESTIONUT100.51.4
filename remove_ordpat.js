const fs = require('fs'); let c = fs.readFileSync('index.html', 'utf8'); const st = c.indexOf('<div id="ordpatView"'); if (st !== -1) { const nd = c.indexOf('<div id=', st + 10); if (nd !== -1) { fs.writeFileSync('index.html', c.slice(0, st) + c.slice(nd)); console.log('Removed ordpatView from HTML'); } else console.log('Failed to find end of ordpatView'); } else console.log('ordpatView not found in HTML');
